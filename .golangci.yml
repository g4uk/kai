version: "2"

run:
  timeout: 3m

linters:
  default: none
  enable:
    - errcheck
    - govet
    - staticcheck
  exclusions:
    rules:
      - path: "_test\\.go"
        linters:
          - errcheck
        text: "t\\.(Log|Error|Fatal|Errorf|Fatalf|Logf)"
