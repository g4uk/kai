run:
  timeout: 3m

linters:
  disable-all: true
  enable:
    - errcheck
    - govet
    - staticcheck

issues:
  exclude-rules:
    - path: "_test\\.go"
      linters:
        - errcheck
      text: "t\\.(Log|Error|Fatal|Errorf|Fatalf|Logf)"
